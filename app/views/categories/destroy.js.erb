$('.delete_category').bind('ajax:success', function() {
    $(this).closest('.category').addClass('animated bounceOut');
    $(this).closest('.category').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', function () {
        $(this).detach();
    });
    $('#container').trigger('update');
});