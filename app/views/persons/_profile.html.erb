<div class="navbar-brand">
<% if user_signed_in? %>
    <span>Welcome, <%= current_user.email %></span>
    <%= link_to '', destroy_user_session_path, :method => :delete, class: 'glyphicon glyphicon-share' %>
<% else %>
    <%= link_to 'Login', new_user_session_path, class: 'glyphicon glyphicon-log-in' %>

      <%= link_to 'Registration', new_user_registration_path, class: 'glyphicon glyphicon-user' %>
<% end %>
</div>